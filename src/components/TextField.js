import React from 'react';

const TextField = ({style, name, type, placeholder}) => {
    <input className={style} name={name} type={type} placeholder={placeholder}></input>
};

export default TextField;